{
  "name": "greenzaction-blockchain",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "test": "hardhat test",
    "node": "hardhat node",
    "balance": "ts-node scripts/balance.ts",
    "deploy:amoy": "hardhat run --network amoy scripts/deploy.ts",
    "verify:amoy": "hardhat verify --network amoy",
    "interact:amoy": "ts-node scripts/interact.ts",
    "deploy:token:amoy": "hardhat run --network amoy scripts/deploy_token.ts",
    "mint:token:amoy": "ts-node scripts/mint_token.ts"
  },
  "devDependencies": {
    "hardhat": "^2.20.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
